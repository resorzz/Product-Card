import React from 'react';
import { Typography, Container } from '@mui/material';

export default function CartPage() {
  return (
    <Container sx={{ mt: 4 }}>
      <Typography variant="h4">Carrito de Compra</Typography>
    </Container>
  );
}